import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.i1.v1.mbwp.v2.znzn',
  appName: "Zene and Zeanne Beat Energy Gap",
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
